---
description: Use Cursor Pet MCP tools; map Agent work stages to Codex pet animations.
---

# Cursor Pet — Agent 行文 ↔ 动作

桌面宠用 Codex 精灵表 **9 个标准动作**（V2 另有朝向行，Agent 不用）。  
有进度变化时调用 `cursor_pet_say`（短中文 + `reaction`）；纯换表情可用 `cursor_pet_react`。

## 动作对照（必须按阶段选用）

| Agent 在做什么 | reaction | 默认气泡感 |
|---|---|---|
| 开场 / 打招呼 | `waving` | 开工 / 你好 |
| 想方案 / 分析需求 | `thinking` 或 `review` | 在想方案… |
| 读文件 / 审代码 / 查文档 | `review` | 在审阅… |
| 改代码 / 写文件 | `editing` 或 `running-right` | 在改代码… |
| 搜仓库 / 多处探索 | `running-left` | 在搜仓库… |
| 跑 shell / 构建 / 安装 | `running` 或 `working` | 在跑命令… |
| 跑测试 / 验证 | `testing` 或 `jumping` | 在测试… |
| 等用户确认 / 阻塞 | `waiting` | 等你确认… |
| 小成功 / 阶段完成 | `success` 或 `waving` | 搞定！ |
| 大里程碑 | `celebrating` 或 `jumping` | 完成啦！ |
| 报错 / 失败 | `error` 或 `failed` | 出错了 |
| 空闲 / 收工 | `idle` | （可不说话） |

官方行序：`idle` → `running-right` → `running-left` → `waving` → `jumping` → `failed` → `waiting` → `running` → `review`。别名已映射到同一行。

## 用法

- 优先 `cursor_pet_say`：`message` 说清「正在做什么」，并带上对应 `reaction`。
- 消息短、对用户可读、≤140 字；禁止代码、日志、密钥、路径、URL。
- 阶段切换时更新一次即可，不要逐步刷屏。
- 宠物不可用时照常干活，不要因此失败。
